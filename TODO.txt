general:
	strip/replace character method so model doesnt have to be updated constantly

all:
	more progressbars where possible

tokenizer:
	get tokens from hocr also
	tokeninfo column in csv (string, hocr, ?)
	--language argument to configure nltk, tesseract, et al
	squish abbreviations? something like below?
	mwes = [
		('P', 'E'), # Politiets Efterretninger
		('d', 'M'), # denne Maaned
		('s', 'M'), # samme Maaned
		('f', 'M'), # forrige Maaned
	]
	mwe = nltk.tokenize.MWETokenizer(mwes, separator='.')
	words = mwe.tokenize(words)

heuristics:
	make make_settings interactive?
