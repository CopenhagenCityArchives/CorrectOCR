all:
	organize all caching in __COCRcache__

dictionary:
	names, places
	prefilter tokens to avoid needles k-bests?

tokenizer:
	punctuationME -- gold should have begin/end punctuation reapplied from original
	dehyphenation: create containertoken to hold the partial tokens
	squish abbreviations? something like below?
	mwes = [
		('P', 'E'), # Politiets Efterretninger
		('d', 'M'), # denne Maaned
		('s', 'M'), # samme Maaned
		('f', 'M'), # forrige Maaned
	]
	mwe = nltk.tokenize.MWETokenizer(mwes, separator='.')
	words = mwe.tokenize(words)

hocr:
	improve segmentation/columnization
	--build_pdf --images 1,2,3 --pdfname etc
